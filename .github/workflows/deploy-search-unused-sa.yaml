name: Deploy search unused service account

on:
  push:
    branches:
      - main
    paths:
      - 'search-unused-sa/**'
      - '.github/workflows/deploy-search-unused-sa.yaml'

jobs:
  deploy-functions:
    name: Deploy audit alert in org aggregation logs
    runs-on: ubuntu-20.04

    permissions:
      contents: "read"
      id-token: "write"

    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@v1
        with:
          workload_identity_provider: "projects/833261298513/locations/global/workloadIdentityPools/github-actions/providers/github"
          service_account: "gha-account@ureuzy-org-system.iam.gserviceaccount.com"

      - name: Set up Cloud SDK
        uses: 'google-github-actions/setup-gcloud@v1'

      - name: Use to deploy a cloud function gen 2
        run: >
          gcloud functions deploy search-unused-sa
          --gen2
          --trigger-http
          --runtime="go120" 
          --region="asia-northeast1" 
          --source="search-unused-sa"
          --entry-point="Main"
          --memory="128Mi"
          --timeout="60s"
          --env-vars-file="./search-unused-sa/env.yaml"
          --ingress-settings="all"
          --set-secrets="SLACK_WEBHOOK=projects/ureuzy-org-system/secrets/slack_webhook:latest"
          --service-account="search-unused-sa@ureuzy-org-system.iam.gserviceaccount.com"